"use strict";

exports.__esModule = true;
exports.theme = exports.plugin = void 0;
const theme = {};
exports.theme = theme;

const plugin = ({
  theme,
  addComponents
}) => {
  const core = theme('rwCore');
  const input = theme('rwInput');
  addComponents({
    ".rw-listbox": {
      "borderRadius": input.borderRadius,
      "backgroundColor": input.backgroundColor,
      "border": `${input.borderColor} 1px solid`,
      "&.rw-state-focus": {
        "@apply rw-focus-ring": "true",
        "color": input.focusColor,
        "backgroundColor": input.focusBackgroundColor,
        "borderColor": input.focusBorderColor,
        "boxShadow": input.boxShadow && core.focusShadow ? `${core.focusShadow}, ${input.boxShadow}` : null
      },
      "&.rw-state-disabled > *,\n      fieldset[disabled] & > *": {
        "pointerEvents": "none"
      },
      "&.rw-state-readonly > *": {
        "cursor": "inherit"
      }
    }
  });
};

exports.plugin = plugin;