export const theme = {};
export const plugin = ({
  addComponents
}) => {
  addComponents({
    ".rw-popup > .rw-time-input": {
      "alignSelf": "center",
      "margin": "theme('margin.3') 0"
    }
  });
};