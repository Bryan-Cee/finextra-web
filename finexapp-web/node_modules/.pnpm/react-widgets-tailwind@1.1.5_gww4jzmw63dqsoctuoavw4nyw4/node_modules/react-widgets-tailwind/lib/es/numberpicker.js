export const theme = {};
export const plugin = ({
  addComponents
}) => {
  addComponents({
    ".rw-number-picker-spinners": {
      "@apply flex flex-col": "true",
      ".rw-btn": {
        "flex": "1 1 0"
      }
    }
  });
};